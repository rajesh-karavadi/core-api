replicaCount: 1

image:
  repository: core-api
  tag: "1.0.0"
  pullPolicy: Never

containerPort: 8080

service:
  type: ClusterIP
  port: 80

env:
  - name: SPRING_PROFILES_ACTIVE
    value: dev

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi